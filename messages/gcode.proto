syntax = "proto2";

package gcode;

message command {
  required opcode op = 1;
  optional int32 arg = 2;
  
  enum opcode {
    HOME = 0;
    MOVE = 1;
    ABSOLUTE_POSITION = 2;
    LOAD_TIP = 3;
    CHECK_TIP_PRESSENCE = 4;
    }
}
